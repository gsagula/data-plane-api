syntax = "proto3";

package envoy.api.v2.core;

import "validate/validate.proto";

/**
 * HTTP status code.
 */
message HttpStatus {
  /**
   * HTTP response codes.
   */
  uint32 code = 1
      [(validate.rules).uint32 = {gte: 100, lt: 600}, (validate.rules).message.required = true];
}
